[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fraud-c2n-runner"
version = "0.1.0"
description = "Experiment runner and plotting for fraud C2N paper"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
